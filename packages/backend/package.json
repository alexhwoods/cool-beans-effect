{
  "name": "@collector/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "start": "bun run dist/index.js",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@collector/shared": "workspace:*",
    "effect": "^3.10.0",
    "@effect/rpc": "^0.43.0",
    "@effect/rpc-http": "^0.42.0",
    "@effect/schema": "^0.75.0",
    "@effect/platform": "^0.67.0",
    "@effect/platform-node": "^0.62.0",
    "@effect/platform-bun": "^0.48.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
