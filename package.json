{
  "name": "cool-beans",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "clean": "bun run --filter '*' clean",
    "typecheck": "bunx tsc --build --force"
  },
  "devDependencies": {
    "@effect/language-service": "^0.47.0",
    "bun-types": "latest",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@effect/rpc": "^0.71.1",
    "@effect/platform": "^0.92.1"
  }
}
