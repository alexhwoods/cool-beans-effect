{
  "name": "collector",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "clean": "bun run --filter '*' clean"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "dependencies": {
    "@effect/rpc": "^0.71.1",
    "@effect/platform": "^0.92.1"
  }
}
